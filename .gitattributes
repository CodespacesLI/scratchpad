# Einheitliche Zeilenenden auf jedem Betriebssystem. Ohne diese Datei macht
# core.autocrlf=true (Windows-Standard) aus Skripten CRLF-Dateien, die in Linux,
# macOS und im Dev Container nicht mehr laufen.
* text=auto eol=lf
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.pptx binary
*.png binary
