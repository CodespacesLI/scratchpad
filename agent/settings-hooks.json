{
  "statusLine": {
    "type": "command",
    "command": "node \"${CLAUDE_PROJECT_DIR:-.}/<AGENTENORDNER>/statuszeile.js\""
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "for p in python3 python; do \"$p\" -c '' 2>/dev/null && exec \"$p\" \"${CLAUDE_PROJECT_DIR:-.}/<AGENTENORDNER>/hooks/guard_test_quality.py\"; done; echo 'scratchpad: kein lauffaehiges Python (python3/python) gefunden' >&2; exit 1"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "for p in python3 python; do \"$p\" -c '' 2>/dev/null && exec \"$p\" \"${CLAUDE_PROJECT_DIR:-.}/<AGENTENORDNER>/hooks/guard_tdd.py\"; done; echo 'scratchpad: kein lauffaehiges Python (python3/python) gefunden' >&2; exit 1"
          },
          {
            "type": "command",
            "command": "for p in python3 python; do \"$p\" -c '' 2>/dev/null && exec \"$p\" \"${CLAUDE_PROJECT_DIR:-.}/<AGENTENORDNER>/hooks/guard_plan_drift.py\"; done; echo 'scratchpad: kein lauffaehiges Python (python3/python) gefunden' >&2; exit 1"
          },
          {
            "type": "command",
            "command": "for p in python3 python; do \"$p\" -c '' 2>/dev/null && exec \"$p\" \"${CLAUDE_PROJECT_DIR:-.}/<AGENTENORDNER>/hooks/guard_context_budget.py\"; done; echo 'scratchpad: kein lauffaehiges Python (python3/python) gefunden' >&2; exit 1"
          }
        ]
      }
    ]
  }
}
